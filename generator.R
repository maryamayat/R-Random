# Generate a random number from Standard Normal Distribution
x<-rnorm(1)
print(paste("rand number (normal)=", x))

# Generate a random number from Uniform Distribution
x<-runif(1)
print(paste("rand number (uniform)=", x))